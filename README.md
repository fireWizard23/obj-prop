A clear and simple way to validate and manipulate properties of an object.

    const { hasNoProperties } = require("obj-prop");
    const test = {}; 
    console.log(hasNoProperties(test)) // true

    